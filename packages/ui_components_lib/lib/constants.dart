/// Default duration for message display.
const defaultMessageDisplayDuration = Duration(seconds: 4);

/// Default debounce duration for info messages.
const defaultInfoMessageDebounceDuration = Duration(seconds: 1);

/// Default debounce duration for successful messages.
const defaultSuccessfulMessageDebounceDuration = Duration(seconds: 1);

/// Default debounce duration for error messages.
const defaultErrorMessageDebounceDuration = Duration(seconds: 15);
